# Copy this file to .env.local (already git-ignored) and fill in real values
# for local development. Never commit real keys — in Framer these same values
# are entered as Property Controls on the component instance instead.

VITE_GOOGLE_MAPS_API_KEY=
VITE_SHEET_SPREADSHEET_ID=
VITE_SHEET_GID=0
